#Trying to debug Demultiplexing code specifically to get cut adapt to be ablerun with right most for 5 prime searching and also to remove reads that matched multiple unique adapters. 

#The code runs but I have noticed in the plate the multiplexing log that number of read the log says it searched doesn't seem to match up to the percent of Reed with adopters 
#identified and additionally way more have adapters Or seem to than did before, it's like 80%. And also strangely the unknown plate level demultiplexing fast Q seems to have like two million reads
#Which makes the math even stranger.

wc -l unknown_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop.fastq
9397216 unknown_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop.fastq

# 9,397,216 / 4 = 2,349,304 which is way too many reads to be in the unknown folder???

head -n 50 plate_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop.log

This is cutadapt 5.2 with Python 3.12.11
Command line parameters: -a file:/group/sldmunozgrp/cysto_LMGSeq08-25/Feb_cysto_flu/sup_basecall/plate_barcodes.fasta -O 14 --revcomp -e 0.15 --times 10 --cores=0 --minimum-length 100 --info-file /group/sldmunozgrp/cysto_LMGSeq08-25/Feb_cysto_flu/sup_basecall/cutadapt_outputs/plate_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop_INFO.tsv -o /group/sldmunozgrp/cysto_LMGSeq08-25/Feb_cysto_flu/sup_basecall/cutadapt_outputs/{name}_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop.fastq /group/sldmunozgrp/cysto_LMGSeq08-25/Feb_cysto_flu/sup_basecall/porechop_outputs/FBC73506_fastq_pass_9ee158db_6bd36f99_0_porechop.fastq
Processing single-end reads on 16 cores ...

=== Summary ===

Total reads processed:               5,420,056
Reads with adapters:                 2,804,379 (51.7%)
Reverse-complemented:                1,810,868 (33.4%)

== Read fate breakdown ==
Reads that were too short:             302,141 (5.6%)
Reads discarded as untrimmed:                0 (0.0%)
Reads written (passing filters):     5,117,915 (94.4%)

Total basepairs processed: 2,157,867,458 bp
Total written (filtered):  2,060,891,128 bp (95.5%)

=== Adapter plate01 ===

Sequence: ATTGAGAGATATAACAGA; Type: regular 3'; Length: 18; Trimmed: 59811 times; Reverse-complemented: 39724 times

#I checked to see if the unknown reads have duplicate reads, they do not: 
grep "dna_r10.4.1" unknown_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop.fastq |  awk '{print $1}' | uniq | wc -l
2349304 

#so there are ~2.3 million unique reads in the unknown fastq, which doesnt add up with only 5.4 million reads processed and ~85% having adapters in either the reg or rc direction. Perhaps is related to --times?
#but i found no duplicate read ids in the unknown reads fastq:  (??)

grep "dna_r10.4.1" unknown_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop.fastq |  awk '{print $1}' | uniq -D | wc -l
0

# new notes added 8/14/202 - I now see i was misunderstanding. the rev-comp reads are a fraction of the reads with adapters found, and are not 
# meant to be added to the adapters found % for a total of ~85% of reads with adapters found. there no longer appear ot be any discrepancies

#Um also, the info file has the a similar number of lines as the num of reads stated to have been processed in the log file: 
#5,425,969 lines in info tsv vs 5,420,056 "reads processed" reported in log header

wc -l *.tsv
5425969 plate_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop_INFO.tsv

# so could be that there are around 5,900 non-unique reads that were processed and therefore showed up multiple times in the log

 awk '{print $1}' plate_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop_INFO.tsv | uniq -d | wc -l
5855

#ok yes, correct, nice

#pickup 8/14/2026 

# wait, that doesnt make sense ... uniq -d prints only one copy of each duplicate line, 
# while uniq -D prints every single copy of the duplicate lines. so if there is a difference
# of about 5,900 non-unique reads, then uniq -D | wc -l should show exactly 5,913, 
# but uniq -d | wc -l should show half of that or less. 

# note added aug 19: wait im dumb, the extra 5913 lines in the info file ARE the duplicates... 
# so since the first appearance of the read is contained in the total reads processed, only the 2nd+ 
# appearance would be counted in the 'extra' lines in the info file. 
# --the minimum number of reads that must have 2 hits only is 5855 - 68 = 5787, 
# --the max number of reads that would have 2 hits only is 5854
# --so the majority of the dup'ed reads appeared 2x, tho we dont know exactly how many appeared 2x 3x or more

# next objective is to determine if the non-unique reads are present in the demultiplexed reads. TBH, i was expecting there to be 
# of them than just a couple thousand of of a few million  (~6000 our of 5.5 million processed, roughly). But I guess 
# that could still be bad if they were all from the same sample, or from one barcode, one PCR, etc. So I'd prefer to
# still do my best to filter them out

# so to check if the duplicates are ENTIRELY taken out of the combined output all reads sorted by plate, well, or 
# any unknown bins, I would need to get the sum of all reads across all potential unique output bins and compare

BASE=/group/sldmunozgrp/cysto_LMGSeq08-25/Feb_cysto_flu/sup_basecall/cutadapt_outputs

# check top-level unknown fastqs, num of reads not assigned to any plates (mindepth/maxdepth 1 = directly in cutadapt_outputs/)
T1=$(wc -l unknown_FBC73506_fastq_pass_9ee158db_6bd36f99_0_cutadapt_porechop.fastq | awk '{print $1/4}')
2349304

# tier 2: plate-level unknown fastqs, number of reads not assigned to plates but not to any wells (directly in plateXX/, not in a wellXX/ subfolder)
T2=$(cat plate??/plate??_unknown*.fastq | wc -l |  awk '{print $1/4}')
1643881

# tier 3: well-level base reads. ignore unknowns becuase those are left-over from the small/med/large search we are doing away with
T3=$(cat plate??/well??/plate??_well??_F*.fastq | wc -l |  awk '{print $1/4}')
1124730

echo "TOTAL: $T1 + $T2 + $T3 =  $(( (T1+T2+T3) ))"
TOTAL: 2349304 + 1643881 + 1124730 =  5117915

# if you add in the reads that were filtered out by cutadapt becuase they were too short, you get the total reads processed
5117915 + 302141 = 5420056 

# ok so what does this mean for whether duplicated reads are being excluded from the output ....

